version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd project8x-website
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: project8x-website/build
    files:
      - '**/*'
  cache:
    paths:
      - project8x-website/node_modules/**/*